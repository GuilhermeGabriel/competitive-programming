#include <bits/stdc++.h>

using namespace std;
#define ff first
#define ss second
#define pb push_back
#define vec vector

double 

int main(){
	std::ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	double n,a,i; vect<int> t;
	while(cin>>n>>a&&n||a){
		cin>>i; t.pb(i);
		sort(t.begin(), t.end());
		
	}
	
	return 0;
}