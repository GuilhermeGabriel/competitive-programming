#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main(){
	ll n, i; cin>>n;
	ll s=0, vi=0;
	vector<ll> estr;

	while(n--){
		cin>>i; 
		estr.push_back(i);
		s+=i;
	}

	while(act>=0&&act<estr.size()){

	}

	//cout<<vi<<" "<<s<<endl;
	
	return 0;
}