#include "bits/stdc++.h"

using namespace std;

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	
	string a,b;
	cin>>a>>b;
	if(a.size()>=b.size())cout<<"go\n";
	else cout<<"no\n";

	return 0;
}
