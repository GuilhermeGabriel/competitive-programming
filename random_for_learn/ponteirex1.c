#include <stdio.h>

int main() {
    int *p;

    // p valor que o ponteiro armazena (que é um endereco)
    // &p endereco do ponteiro
    // *p valor da variavel que tá no endereco que o ponteiro armazena

    return 0;
}