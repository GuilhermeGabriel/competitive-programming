#include <bits/stdc++.h>

using namespace std;

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);

	int n,in; pair<int,int> tmp;
	vector<pair<int,int>> arvs;
	
	while(n--){
		cin>>tmp.first>>tmp.second; 
		arv.pb(tmp);
	}

	sort(arvs.begin(), arvs.end());
	

	return 0;
}